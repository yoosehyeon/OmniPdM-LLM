--extra-index-url https://download.pytorch.org/whl/cpu

# Core ML
torch>=2.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
huggingface_hub>=0.24

# UI (Dash stack)
dash>=2.18
dash-bootstrap-components>=1.6
plotly>=5.24
# werkzeug 은 Dash → Flask transitive 의존이지만, services/auth/passwords.py 가 직접
# 사용하므로 명시. 3.x 부터 generate_password_hash 기본이 scrypt.
werkzeug>=3.0

# LLM (Groq OpenAI-compatible)
openai>=1.40

# Experiment tracking
mlflow>=2.16

# Visualization (legacy matplotlib kept for training scripts only)
matplotlib>=3.7

# Dataset loaders
# - scipy: CWRU .mat 로더 (data_pipeline.py:262) — sklearn 의존성으로 자동 설치되지만 명시
# - h5py: N-CMAPSS .h5 로더 (data_pipeline.py:596) — sklearn 으로 안 들어옴, 별도 필수
scipy>=1.10
h5py>=3.10

# Tier 1 — Realtime worker (PRD v6.2 §10 T1-01). services/realtime/* 사용 시 필수.
# T1-02 알람 채널은 stdout 기본. 외부 채널(Telegram/Email/Slack 등)을 붙일 때 해당 dep 추가.
paho-mqtt>=2.1

# Tier 1 — DbWriter (PRD v6.3 §10 T1-03). TimescaleDbWriter 사용 시 필수.
# psycopg[binary] = libpq 자동 포함, Windows 에서도 추가 설치 없음.
# NullDbWriter 로 갈 때는 미설치라도 동작 (db_writer 안 import 자체는 통과).
psycopg[binary]>=3.2

# Notebook-only (선택): seaborn 은 notebooks/* 에서만 사용. 학습/UI/워커 런타임 미사용.
# 필요시: pip install seaborn>=0.13
