--extra-index-url https://download.pytorch.org/whl/cpu

# Core ML
torch>=2.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
huggingface_hub>=0.24

# UI (Dash stack)
dash>=2.18
dash-bootstrap-components>=1.6
plotly>=5.24

# LLM (Groq OpenAI-compatible)
openai>=1.40

# Experiment tracking
mlflow>=2.16

# Visualization (legacy matplotlib kept for training scripts only)
matplotlib>=3.7

# Tier 1 — Realtime worker (PRD v6.2 §10 T1-01). services/realtime/* 사용 시 필수.
# T1-02 알람 채널은 stdout 기본. 외부 채널(Telegram/Email/Slack 등)을 붙일 때 해당 dep 추가.
paho-mqtt>=2.1
